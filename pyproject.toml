[project]
name = "muon-routing-atlas"
version = "0.1.0"
description = "Single-GPU experiments for routing Muon across GPT transformer parameters."
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pyright]
pythonVersion = "3.10"
typeCheckingMode = "basic"
